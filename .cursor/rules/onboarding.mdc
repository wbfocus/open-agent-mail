---
description: 这是 Agent Mail 运用项目；先绑用户自己的邮箱，再按场景请他转发邮件
alwaysApply: true
---

这是 Agent Mail 的运用项目。信用卡账单只是其中一个场景。

克隆后的用户，先完成这两步，完成前不要读邮箱，也不要开始做场景：

1. 要求用户绑定他自己的 Agent Mail：`agently-cli auth login`，然后 `agently-cli +me`。必须是用户自己的邮箱。未登录成功就停住。
2. 告诉用户目前的场景：可以把招商银行或民生银行的信用卡电子账单转发到这个 Agent Mail。转发后由助手下载到 `bills/`，运行 `python scenarios/credit-card/build_ledger.py`，生成同一商户加总、一笔不漏的 HTML 看板。

不要提交账单原件和生成的看板。
